import React from "react";

const Header = () => {
  return (
    <section className="headerContainer">
      <section className="header__wrapper">HEADER</section>
    </section>
  );
};

export default Header;
